apiVersion: 1

providers:
  - name: "notes-app"
    orgId: 1
    folder: "Notes App"
    folderUid: "notes-app-folder"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards